'use strict';
var categoryControllers = angular.module('categoryControllers', []);

categoryControllers.controller('CategoryIndexController', 
  function ($scope, $stateParams, CategoryService) {
    
  }
);
